from setuptools import setup

setup(
  name='tusk',
  version=0.1,
  description='This is a test package to demonstrate a good repository',
  packages=['tusk'],
  license='MIT',
  install_requires=[
    'six'
  ]
)